apiVersion: apps/v1
kind: Deployment
metadata:
  name: orcacompute-db
  namespace: orcacompute
  labels:
    app: orcacompute-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orcacompute-db
  template:
    metadata:
      labels:
        app: orcacompute-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: orcacompute-secrets
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: orcacompute-secrets
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: orcacompute-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: orcacompute-secrets
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: orcacompute-db
  namespace: orcacompute
spec:
  selector:
    app: orcacompute-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
