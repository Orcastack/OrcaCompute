apiVersion: v1
kind: Service
metadata:
  name: orcacompute-frontend
  namespace: orcacompute
  labels:
    app: orcacompute-frontend
spec:
  selector:
    app: orcacompute-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
  type: ClusterIP
