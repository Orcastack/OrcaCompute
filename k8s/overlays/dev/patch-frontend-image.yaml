apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: orcacompute
spec:
  template:
    spec:
      containers:
      - name: frontend
        image: atonixdev/orcacompute-frontend:1.0.0
