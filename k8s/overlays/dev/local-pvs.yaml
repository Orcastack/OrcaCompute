apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  namespace: orcacompute
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /var/lib/orcacompute/postgres
  persistentVolumeReclaimPolicy: Retain
  storageClassName: fast-ssd
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv
  namespace: orcacompute
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /var/lib/orcacompute/redis
  persistentVolumeReclaimPolicy: Retain
  storageClassName: fast-ssd
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: media-pv
  namespace: orcacompute
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /var/lib/orcacompute/media
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs
