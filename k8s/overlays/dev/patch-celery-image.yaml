apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-worker
  namespace: orcacompute
spec:
  template:
    spec:
      containers:
      - name: celery-worker
        image: atonixdev/orcacompute-backend:1.0.1
