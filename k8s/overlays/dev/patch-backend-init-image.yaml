apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: orcacompute
spec:
  template:
    spec:
      initContainers:
      - name: migrate
        image: atonixdev/orcacompute-backend:1.0.1
